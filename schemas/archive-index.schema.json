{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmm.local/schemas/archive-index.schema.json",
  "title": "MMM Archive Index",
  "type": "object",
  "required": ["schemaVersion", "generatedAt", "totalMixes", "items"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "type": "string" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "totalMixes": { "type": "integer", "minimum": 0 },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "slug", "title", "displayTitle", "publishedAt", "summary", "trackCount", "path"],
        "properties": {
          "id": { "type": "string" },
          "slug": { "type": "string" },
          "title": { "type": "string" },
          "displayTitle": { "type": ["string", "null"] },
          "publishedAt": { "type": "string", "format": "date-time" },
          "summary": { "type": "string" },
          "trackCount": { "type": "integer", "minimum": 0 },
          "favoriteCount": { "type": "integer", "minimum": 0 },
          "path": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
